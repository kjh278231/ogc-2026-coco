# 실험 보드 — 살아있는 상태판 (계획 / 진행 중 / 완료)

> 솔버 실험의 **현재 상태 인덱스**: 무엇이 대기 중이고, 무엇이 실행 중이며, 무엇이 결론났는지.
> 상세 서술·수치는 `docs/experiment_log.md`(BRIDGE)와 `docs/third_algorithm_experiment_log.md`(covering)에,
> 오래 가는 결론은 `memory/`에 있다. **이 보드는 앞으로 할 것 + 지금 도는 것 추적용**이다.
> 항상 최신으로 유지할 것: 실험 시작 → 진행 중, 끝나면 → 완료(결론과 함께), 새 아이디어는 → 계획.
> **평가 프로토콜(필수): 모든 A/B는 eval-count 고정(`SOLVER_MAX_EVALS=N`)으로 측정**(결정론적; wall은 20~66% 변동) **+ wall 시간 별도 보고**. 상세 `docs/methodology.md` §1b.

**마지막 업데이트: 2026-07-04 (신규 목표: 기존 6솔버 best-of가 아닌 제3의 별개 모델 — PILLAR(price-directed column generation) 설계 + cheap-falsification 프로브 실행 중, `docs/pillar_design.md`)**

---

## 🔵 진행 중 (지금 실행 중)

| 시작 | 실험 | 설정 | 출력 파일 | 무엇을 결정하나 |
|---|---|---|---|---|
| 07-04 | **제7 솔버: PILLAR(CG) 반증 → ACCORD(per-block 가격 합의) 프로브 PASS·구현·검증 중** | 설계+반증 체인 `docs/pillar_design.md`. ▶**PILLAR(CG) 반증 확정(4 프로브)**: T17/T13 수렴(E2000)·T6+Neame smoothing 0.7·T13 미수렴(E600) 전부 — pricing이 neg-RC 컬럼은 매 라운드 발견(**RC −23.1만/목적값 19.9만**)하나 **LP +0.00%·IP/true 개선 0**. RC 공식 self-check 검증(버그 아님). **원리**: m=4~5 거대(60+블록) 중첩 컬럼 partition에서 신규 컬럼은 여집합 커버 부재로 **좌초** — CG 적정영역(작은 컬럼 다수)의 정반대. 부수 발견: 미수렴 incumbent(E600)서도 pool-only recombine 개선 0 = 단일 궤적 pool은 전부 근-incumbent(recombine의 역사적 승리는 cross-basin pool 필요). → memory `cg-column-pricing-falsified`. ▶**피벗 ACCORD = {가격증강 배정 MIP → 진짜 per-bay 패킹 → per-block 혼잡가격 갱신} 루프**(ADMM/surrogate-Lagrangian/tatonnement; PRISM 정적 λ-앵커는 무피드백 특수경우). **프로브 PASS(동일예산 정적 λ-그리드 대비)**: **T13 955,949→200,288(4.8×, 2s)·T20 2,103,329→543,671(3.9×, 5s)**, 무수선 raw MIP 이터레이트로 Z1 94→0(T13)·261→9(T20). it6+ 진동(overshoot)=다음 레버. ▶**구현**: `accord/{myalgorithm,accord_engine}.py`(+`tools/_accord_run.py`·`_entry_run.py`) — adaptive ρ(악화×0.6/개선×1.05)·incumbent backtrack(>1.3× 폭주 시)·stall-6 seeded jitter·a_pref floor·dynamic poly reserve(best Z1=0이면 4%로 축소, 단조라 무회귀)·`ACCORD_ITERS` 결정론 knob. 커널 재사용=패커/빌더만(PRISM 방식), 내부 LAHC/ILS/recombine 없음. ▶**감쇠 스윕(T13@60, 챔피언 PRISM 4코어=114,810)**: default(.7/1.0) 156,974 → decay.9/ρ.3 121,810 → **+prox2(ADMM proximal) 112,234 Z1=0 = 단일코어로 챔피언 격파** (d95/ρ.2+prox2 115,746은 열세→기본값 확정). **blockers 과금 v1 기각**(152,325, 가격신호 오염). ▶**하드 패밀리 T=60 검증(단일 wall run, 전부 oracle-feasible stage5)**: **T13 −2.2%승·T18 −3.6%승**(T18=PRISM 역대 문제 인스턴스), T17 +9.7%·T11 +27%·T20 +25% 패, T1 +377%(Z1=1 결정론 트랩 — per-block 가격은 피해자만 지목, 해법은 in-bay 재배열=blocker형 신호 필요) = **첫날 2승4패, 신규 패러다임 기준 강력**([[new-mechanism-long-view]]). ▶**dynamic reserve 재검증**: wall 39→54s·iter +30~50%인데 **best 전부 동일 → 병목=예산이 아니라 탐색 다양성**(price-jitter가 도달 가능 basin 조기 포화). 차기 레버 순위: ①가격공간 restart-diversity(재시드) ②stall 중 λ-스윕 ③4코어 가격루프 포트폴리오 ④blocker-shaped 신호(T1류). 상세 `docs/accord_experiment_log.md` | `.claude/scratch/_cg_probe.py`·`_cg_{T6,T13e600}.log`·`_accord_probe.py`·`_accord_{T13,T20}.log`·`_acc2_*.log`·`_val{,2}_*_60.log`, `docs/pillar_design.md`·`docs/accord_experiment_log.md` | **사용자 신규 목표(07-04): 기존 6솔버 best-of 아닌 제3의 별개 모델 + 최신 연구 적용.** 6솔버 공통 패러다임=trajectory search(+passive recombine) → 미개척 축=능동적 전역 생성. CG(EJOR2025·MPC2023·Wentges/Pessoa·arXiv2604.23889) 죽고 ADMM-MIP 휴리스틱(Boyd MIQP-ADMM·Mix-CALADIN arXiv2604.14897·Bragin surrogate-LR) 계열이 생존. **남음**: T=180 스팟체크(T13/T20, 실행 중), 탐색 다양성 레버 |
| 07-01 | **Z3 레버 = Z1=0 전환 guided SWAP (`SOLVER_SWAP`) — 검증·기본ON·zip 제작, 그래더 재제출 대기** | `bridge/solver.py:_z3_refine`(상호 선호불일치 쌍만 값싸게 후보→multi-order total_obj로 Z1=0 유지 확인→first-improving, Pareto-safe·결정론). `prism/prism_engine.py:_refine` LAHC 수렴점마다 호출(gated). `prism/myalgorithm.py` 기본 ON. zip `myalgorithm0701-prism-mo-swap.zip` | `tools/_swap_probe*.py`·`_prism_swap_run.py`, `.claude/scratch/_swap_final_checks.txt` | **사용자 아이디어(07-01): "Best가 Z1=0 되는 시점부터 전략 전환".** 목적함수 near-lex라 Z1=0 후엔 Z3 최소화 게임인데 탐색은 relocation만·recombine도 본 조각만→상호선호교환 못 잡음. **swap이 그걸 포착.** ▶**cheap-falsification**: 약베이스 −10~37%, **강베이스(recombine포함 full pipeline, E3000)서도 −6.4~−54.4%**(전부 Z3), **PRISM+MO 실장 eval T13 swap OFF 404238→ON 117545(−71%)**. ▶placement 진단서 Z1=maxed(finals Z1=0)·position −6%·orientation dead 확인 후 Z3로 피벗한 결과. **남음**: zip smoke(feasible·overrun 진행 중)→wall 포트폴리오 A/B→그래더 재제출(P3/P6 Z3 추가개선 기대). [[z3-swap-refine]] [[placement-lever-diagnosis]] [[grader-best-0619-2]] |
| 07-01 | **적치 개선 = multi-order 패커 + 신규 솔버 STOW(packing-diverse portfolio) — STOW 작동 확인, 3-way T=180 A/B 진행 중** | `bridge/packing.py`: `solve_bay(order=)`+`solve_bay_best`(EDD 먼저, tardy면 best-of, EDD포함→Pareto-safe, `SOLVER_MO_ORDERS`로 order세트 가변[기본4, 2-order로 throughput절감 가능]). `bridge/solver.py:_MULTIORDER`(eval·guard·build 3곳, 기본OFF=bit-identical, PRISM도 자동상속). 신규 `stow/myalgorithm.py`=bridge 하니스 재사용, 워커 다양성축=패킹정책{multiorder×2, EDD×2}, master는 multiorder 빌드 | `tools/_pack_diag.py`·`_order_probe.py`·`_interlock_ceiling.py`·`_mo_{run,wall_ab,eval_ab}`·`_stow_run.py`·`_solver3_ab.sh`, `.claude/scratch/_{mo_wall60,mo_eval2000,solver3_T180}.txt`, `docs/stow_experiment_log.md` | **사용자 신규 목표(07-01): 신규 기법 + 적치 개선.** ▶**cheap-falsification**: ① **temporal_floor(Z1)=0 전부**→Z1=100% 혼잡. ② demand_peak_util이 tardy bay 둘로 가름(포화>1.0=배정레버 vs 여유<0.85=적치여지). ③ **맞물림 천장 falsify**: 포화 bay는 floor면적으로도 포화(T20-bay1 1.75→1.56), floor_util 8~13%만↓ → feasibility-critical 대비 천장 작음=비우선. ▶**order 레버**: bay-oracle Z1 −24%, release단독 −19%. ▶**multi-order A/B 핵심**: **고정-eval(결정론) E2000 전부 큰 승**(T2−26·T13−36·**T6−64**·T20−15·T18−19), 단 **wall T60 single은 6승3패**(승 T1−91·T9−52·T11−48·T13−24·T14−13, "패" T6/T20/T18 = **eval서는 −64/−15/−19% 승** → wall회귀는 품질 아닌 **throughput**[multiorder per-eval 2~3×느림, 궤적 비단조]). → **블랭킷 기본값 부적합, best-of 포트폴리오가 정답**(best-of(EDD,multiorder) wall 6승0패). ▶**STOW**=그 vehicle(4워커: multiorder×2+EDD×2, master는 multiorder 빌드). ▶**3-way T=180 A/B(진짜 obj) 결과 — STOW 검증**: T1 **15611**(vs prism32188/bridge41024, −52%)·T9 **51805**(−25%)·T13 **131606**(−22%)·T20 **221532**(≈prism) **STOW 4승**, T11 51282(=bridge, **MIP앵커 부재**로 prism38262에 패)·T18 78656(bridge68433에 패, 다양성 희생). **집계 STOW 550292 < PRISM 605131 < BRIDGE 665545 = STOW 최강(vs prism −9%, bridge −17%).** ▶**✅ PRISM+MO = 전 솔버·전 인스턴스 제패(결정적)**: T=180 4-way(진짜 obj, 6 하드) **PRISM+MO 6/6 전승** — T1 6533(prism32188 대비 −80%)·T11 **32039**(앵커가 STOW의 T11갭 해결)·T13 111752(−34%)·T18 **65949**(bridge68433도 능가=T18갭 해결)·T20 149050(−33%). **집계 417043 = PRISM −31%·STOW −24%·BRIDGE −37%.** MIP앵커(T11)+multi-order(나머지)가 모든 레버 결합. bridge+MO도 −34%. ▶**적용**: `prism/`+`bridge/myalgorithm.py`에 `SOLVER_MULTIORDER=1` 기본 ON(spawn 워커 env 상속, fallback pop). ▶**✅ T=60 짧은예산 검증(PRISM vs PRISM+MO) 6/6 무회귀**: T1 −52·T2 −26·T6 −19·T13 −45·T17 −22·T20 −35%, 전부 feasible·wall≤55.9s(no overrun) → throughput 우려는 포트폴리오서 해소(짧고 긴 예산 모두 대승). ▶**✅ 제출 zip 게이트 통과**(`myalgorithm0701-prism-multiorder.zip` 추출본): T20@300 148170(wall292.2)·T14@300 94504·T18@300 64859·T2@60 3690 전부 feasible·**overrun無**(margin 7~8s). ▶**✅ 전역 무회귀 검증**: 쉬운/미검증 8개 T=60도 전부 승/동률(T4 **−63%**=과거 PRISM 약점 packing-sensitive 해결·T16 −28·T7 −24·T5 −15·T3 −12·T10/T12 −3~5·T8 동률) → **누적 ~20 인스턴스 전부 승/동률·회귀 0·feasible·overrun無 = train 전역 보편적 개선**. **✅ 그래더 검증 완료(07-01) — PRISM+MO 역대best 5/6 경신**: P1 11280(동률)·P2 **33712(−32.6%)**·P3 **210445(−57.5%)**·P4 **7025792(−12.1% WIN, PRISM 유일 약점 +8.6%를 뒤집음!)**·P5 **19930750(−10.7%)**·P6 47561554(+0.2%, 유일 소폭 회귀). train 예측(P3-P6존 −33%)이 그래더서 대실현. **multi-order=역대 최대 단일 그래더 레버.** 새 anchor=P1-P5 PRISM+MO/P6 0629 PRISM. **남음**: P6 회복(snapshot-guard[비단조 흡수] 또는 mixed-packing PRISM[EDD 워커로 best-of 가드]; P6는 그래더-only 재현불가라 재제출로 검증). STOW(`stow/`)=신규 폴더·개념증명. ▶**2-order 기각**(4-order vs `SOLVER_MO_ORDERS="edd,release"` @180: 4-order가 T11/13/18/20 −9~23% 우수=order 다양성>throughput, 4-order 값 재현=결정론). 차기 레버=snapshot-guard(비단조 흡수)·position/orientation 정책(미탐색). [[objective-z3-dominant]] [[eval-count-ab-protocol]] [[guided-destroy-portfolio]] [[grader-best-0619-2]] [[prism-third-algorithm]] [[anchor-to-grader-best]] |
| 06-30 | **PRISM λ-앵커 (단독 성능 개선, best-of 병합 금지) — ✅ config D 채택, zip 제작, 그래더 제출 대기** | `prism_engine._lambdas`: 단일 MIP 앵커 `mip1`→**`mip16`** (`PRISM_LAMBDAS="1"→"16"`); 휴리스틱 trio(pref,balanced,capped) 유지 → 워커 **`{pref,balanced,capped,mip16}`** (env 롤백 가능). zip `myalgorithm0630-prism-lambda.zip` | `.claude/scratch/prism_{lb_gap,anchor_pack,lambda_pack,mip_timing}.txt`, `anchor_ab*.txt`, `cfg_{A,C,D,F}.txt`, `old_diag.txt`, `old_t4t7.txt`, `run_cfg.py`, `tools/_build_prism_zip.py` | **목표(06-30): PRISM 단독을 그래더 50위권에서 끌어올림.** ▶**진단(cheap-falsification)**: ① "MIP 과혼잡"가설 **반증**(mip1 raw Z1 < a_pref). ② **shipped lam=1이 최약 MIP 워커** — λ↑(16)면 부하분산→지배비용 w1·Z1 raw **−50~79%**, 휴리스틱 앵커가 못 가는 Z3-heavy basin 도달. ③ count-cap MIP **반증**. ④ λ16 mip_tl=4s 내 **증명-최적 ≤0.5s 결정적**(공짜 front-load). ▶**워커별 진단(결정적)**: 인스턴스마다 승자 다름 — pref/capped(T11/T20 + **T4/T7 packing-sensitive: pref≡capped≡mip1이 a_pref basin 재시작 복권**)·balanced(T38/T14 heavy 부하분산=P4/P5존)·mip16(T17/T7/T13/T18 Z3-heavy=P3존). ▶**wall 노이즈 발견(중요)**: 워커가 wall-데드라인 기반→machine-load 따라 수렴 변동(동일 mip16 워커가 T13 125,371×3 vs 168,579×1; pref 워커 T11 38k~127k). 단일-run 크기 신뢰 불가, **방향성만 robust**. ▶**config F={pref,balanced,mip8,mip16} 시도→기각**: T4 **+15%** 회귀(a_pref 재시작 시드 1개로 축소). ▶**최종 config D = mip1만 mip16 교체(최소교란)**: 휴리스틱 trio 그대로 → T14/T38(balanced)·T11/T20(pref) 보존, mip16이 **T17 −8.6%·T7 −8.2%·T13 −8~32%·T18 −3% 승**, **T4 +3.4%(F의 +15%보다 훨씬 작음)·T15 +5%만 회귀.** F 대비 T9 승 하나 포기하고 packing-sensitive downside 대폭 축소 → 그래더 P1–P6 구조 정렬 우수. zip 추출-스모크 OK(T60/180/300 feasible·overrun無). **남음**: 그래더 제출 검증(baseline=현 live 0629 PRISM). [[prism-third-algorithm]] [[grader-best-0619-2]] [[objective-z3-dominant]] [[anchor-to-grader-best]] |
| 06-29 | **PRISM = 제3 알고리즘 (preference-ideal MIP 앵커 스펙트럼 + 멤메틱 LAHC, v0 구축·검증 중)** | `prism/{myalgorithm,prism_engine}.py`(커널만 BRIDGE 재사용), 앵커={a_pref,a_balanced,a_pref_capped}∪{argmin(λ·w2Z2+w3Z3):λ∈`PRISM_LAMBDAS`(기본1,8,64)}, 각 LAHC 정제→best-of+recombine | `docs/prism_experiment_log.md`, `tools/_facet_*.py`(probe), `tools/_prism_ab*.txt` | **사용자 신규 목표(06-29): BRIDGE 아닌 제3 알고리즘.** ▶**진단 3종 falsify**: ① 전역 MIP-Benders(no-good/superset soft-penalty cut)=Z1을 0으로 못 몰아감(혼잡 패턴 지수적, whack-a-mole; MIP Z3는 ILS의 5~30배 낮으나 Z1 폭발) ② MIP-LNS(fix-and-optimize)=move당 MIP 너무 느림(T20 30s에 ~150move) ③ **a* 앵커 LAHC=상보적 생존**: a*-seed가 a_pref-seed LAHC 대비 **T20 −6%·T13 −11.6%·T15 −21.9%·T17 −20.1% 승**(둘 다 Z1=0, a*가 Z2도 급감), T5/T10/T11/T14 패(instance-split). **살아남은 자산=global 배정 MIP의 Z3/Z2 파워(실패는 오직 Z1 제어)** → 앵커로 LAHC가 Z1 수선. ▶**PRISM=단일앵커 미집착, λ-스펙트럼으로 다양한 Z1=0 basin 생성→best-of.** ▶**결정성 버그 수정**: MIP앵커 Threads=1·Seed=0(병렬 MIP은 seed에도 비결정, lam=64는 ~3.4s로 TimeLimit 경계), recombine CP_WORKERS=1. **균등분할 직렬=핸디캡**(E=4000÷4앵커=앵커당 1000, a* 과혼잡 Z1 미수선→T4 +105%) → **PRISM 가치는 병렬(앵커당 풀예산) 전제.** ▶**full-budget(앵커당 E=4000=병렬 모사) 전20 결정적: PRISM 10승7패3무, 집계 −0.1%(BRIDGE와 동률), oracle −9.3%.** 승=하드/대형 Z3계열(T9 −31%·T12 −20%·T15 −22%·T14 −15%·T19 −13%·T13 −12%·T17 −11%·T20 −7% = **P3~P6 약점존**), 패=소형 패킹민감(T4/T5/T7, PRISM Z1>0=BRIDGE 파이프라인 우위). covering standalone(+5~13%)보다 월등. ▶**repair-first(local_search 선행) 기각**(net 음수, T6 Z1 0→7). ▶**포트폴리오**(`prism/portfolio.py`, T≥180, 앵커↔코어, 마스터 MIP 1회+union-recombine, 워커 순수 NORECOMB LAHC): **버그 3종 수정**(① import-shadowing=bridge portfolio가 대신 실행→bridge dir append로 수정 ② 조기종료=워커 단일 LAHC plateau→ILS 루프 추가 ③ 워커수집 race=ILS로 gather_dl까지 돌다 kill→serial fallback 참사→collect_margin) + **④ 재시작-다양성(결정적 레버)**: 워커 공유 seed→같은 basin funnel→T1 트랩+167%. 워커별 seed(+mover-shuffle=div01류) 부여로 해결. ▶**결과(wall T=180, PRISM-pf vs BRIDGE-pf, 진짜 그래더 obj, FULL-20): PRISM 13승5패2무, 집계 −6.9%(PRISM가 BRIDGE 능가!)** — 큰 승 T11 −24.9·T1 −21.5·T4 −17.6·T20 −16·T6 −10.6·T15 −10.2·T17 −8.6·T9 −8.3, 작은 패 ≤3%(T3/T5/T12/T16), 유일 큰 패 T18 +19.4. 비하드12는 paired(drift통제)에도 PRISM 광범위 승 → drift 아님. **PRISM이 하드/대형/Z3(P3~P6 약점존) 능가.** zip `myalgorithm0629-prism.zip`(평면6파일, import 스모크 OK). ▶**✅ 그래더 검증 완료(06-30) — train→grader 전이 확인**: 역대 best(BRIDGE 0619-2) 대비 **P1 동률·P2 47,376(−5.4%승)·P3 391,525(−20.9%승)·P4 8,674,414(+8.6%패)·P5 22,538,969(+1.0% 미미)·P6 46,523,170(−2.0%승)** → **best를 P2/P3/P6서 능가, 유일 의미있는 패는 P4(+8.6%)**. 그리고 0629 single-LAHC가 무너진 대형 long-budget **P4/P5/P6을 전부 회복(LAHC 대비 −3.6~5.7%승)**, P6은 best까지 넘음 → train 예측(하드/대형/Z3=P3~P6존 능가)이 그래더에서 성립. **PRISM=역대 최강 단일 제출본.** 약점 P4(+8.6%)·P5(미미)는 ≥300s 최난패킹(T20/T14 계열, MIP앵커 과혼잡) → **combined BRIDGE+PRISM 포트폴리오**가 전 인스턴스 ≥best 경로. **남음**: P4/P5 회복(combined portfolio)·T18 패 규명·체크리스트 P★(유휴)/P★b(병렬)·serial eval 재기준. [[objective-z3-dominant]] [[grader-best-0619-2]] [[prism-third-algorithm]] [[guided-destroy-portfolio]] [[anchor-to-grader-best]] |
| 06-28~29 | **basin-escape = a_pref 신선하강 + 포트폴리오 재설계 (통합완료, 그래더 검증 대기)** | `_climb_lahc`(+`SOLVER_LAHC`기본1/`_L`기본1/`_INIT_FRAC`기본0.2/`_STRICT`), 포트폴리오 재설계(anchor+NORECOMB+spawn-probe) 재활성화 | scratchpad/{bd,pb,map,strict,full,fp2,vs,final,pf3,anc,probe,unguarded}_*.txt | **단일 LAHC = 검증된 대형 레버.** 전20 맵 best-of(L1,L30) vs greedy **14승5무1패**; full 제출본 경로 6/6승 −15~54%; 통제로 동력=**a_pref 신선 단일위상 하강**(neutral≈0). **단일 LAHC vs OLD 제출본 T13/T20 @60/300: −31~78%.** ▶**포트폴리오 #2 재설계**: 앞선 "단일이 포트폴리오 −63~75% 압도"는 [[portfolio-spawn-guard]] **테스트 아티팩트**(unguarded 하베스→재귀 spawn)였음. guard 후 재측정 → 포트폴리오 정상. 재설계=anchor worker(LAHC-L1+recombine=단일, 라이선스 단독)+NORECOMB 다양성 3개+master union-recombine. **guarded T180: T20 −27%·T14 −21%·T6 −1%·T13 +2%**(net 큰 이득, T13만 코어분할로 소폭 회귀). **spawn-probe**로 unguarded 그래더 시 full 단일 fallback(검증 T20=366647=단일). **남음**: 그래더 P1~P6 검증(실제 anchor). zip: `0629-lahc`(단일)·`0629-lahc-portfolio`(단일+포트폴리오). [[lahc-basin-escape-win]] [[new-mechanism-long-view]] [[anchor-to-grader-best]] |
| 06-29 | **LAHC restart-diversity = diverse a_pref 재시드 워커 div01 (채택, zip 제작, 그래더 검증 대기)** | `_climb_lahc(rng=)` mover-셔플 + 메인루프 `SOLVER_LAHC_DIVERSE`(기본0, **bit-identical** 확인: OLD 0629 anc=현재 anc T1/T13 정확 일치, 정리 후 재확인). portfolio.py 워커4 i01→**div01**(L1+DIVERSE+init0.1). zip `myalgorithm0629-divrestart.zip` | tools/_lahc_diverse_ab.py·_lahc_profile_ab.py·_lahc_matrix.txt·_portf_ab*.txt·_portf_varC/D/5w.txt·_portf_more.txt | **사용자 요청 "LAHC 조금 더 개선" → 채택.** ▶**반증 2건**: ① diverse a_pref를 kick *교체*로 쓰면 −33% 악화(T13 E10k: kick 173k vs diverse 260k) — 기존 `_perturb(best)` kick이 이미 다양 재시작+좋은 출발점→단조 체인(282→222→200→182→173k). ② plateau-wander tail은 낭비 아님: `SOLVER_LAHC_PATIENCE`로 자르면 +26~42%(plateau-crossing이 탐색 메커니즘 그 자체) → patience 코드 제거. ▶**상보 멤버로는 강력**(eval-count E10k): diverse가 T1 −49%·T20 −14%, kick이 T13/T26 승 → instance-split([[guided-destroy-portfolio]] 패턴). ▶**실제 경로 wall T180 A/B (확정)**: **T1 −45%(75080→41024, recombine 넘어선 대형 승)·T11 −2.8%·T6/T17/T18/T20 ±0·T13 +9.1%(161210→175818, 반복 결정론적=진짜 회귀).** net=**2승1패4무 → rank-based net-positive, 단 Pareto 아님.** ⚠**핵심 교훈**: eval-count NORECOMB proxy가 (a)recombine 자체의 trap-탈출(T20 등) (b)4워커 풀 union 시너지를 **둘 다 못 봄** → 실제 경로에선 i01이 잉여 아님(유일 기여=T13 161k union 시너지). **T13 161k = 원래 4워커 풀 全전 풀스피드일 때만 나오는 fragile union-recombine 시너지** → 4코어 cap서 div01 넣으려 *아무 워커나* 빼거나(변형 B/C/D 전부 T13 175818) 5워커 오버서브(풀 품질↓로도 175818)면 깨짐. i01=최소비용 희생. **남음**: 그래더 P1~P6 검증(특히 LAHC 약점 P4-P6에 div01 도움 여부). [[lahc-basin-escape-win]] [[anchor-to-grader-best]] [[eval-count-ab-protocol]] [[portfolio-recombine-degraded]] |

---

## 🟡 계획 (우선순위순)

P★ (PRISM, 체크리스트 §1 — 유휴 회수) — **✅ 채택**: 마진 12~20s → **4~6s**. ① master idle-reclaim ILS(`PRISM_PORTF_IDLE_RECLAIM`, guarded/monotonic, poly_deadline degradation으로 overrun-safe) ② `safety` `max(2,0.04T)`→`min(5,max(3,0.025T))`. 검증 T20@180 wall173.9s(margin6.1)·T14@180 174.5s(5.5)·T17@60 55.9s(4.1), overrun 0, obj bit-identical(monotonic). 품질 불변(near-converged+1코어 idle-fill이 4코어 못 이김)이나 무해+미수렴 인스턴스선 도움. zip 재빌드. 더: 워커(4코어) 시간환원=상위 upside(overrun-riskier).
P★b (PRISM, 체크리스트 §2) — **✅ 채택**: 포트폴리오가 직렬을 전 예산서 압도(T=120 T20 −43.6%·T13 −6.5%, T=60 T20 −59.1%·T17 −21.6%·T13 −15%; 직렬 even-split은 굶어 T20 Z1=7~11 패킹실패). 예전 "T=60 회귀"=budget-split V1(무관). **게이트 `PRISM_PORTFOLIO_MIN_T` 180→45**로 P1(≤60)·P2도 4코어. zip 재빌드(게이트45). 남음: 매우 짧은 T(<45) 미검증, 직렬 poly reserve 유휴(~50s)도 별도. RAM 16GB 미활용(풀↑ 여지).

0★ (다음, 이제 입증된 최우선). **탐색 중 incumbent 스냅샷 진짜채점 (monotonic 가드)** — reserve-인하 REJECT(완료 참조)가 직접 입증: **모든 '더 많은 search' 레버(reserve 인하·adaptive-reserve·긴 budget)가 proxy-drift 벽에 막힘** — 환전한 search가 prob_20을 더 나쁜 true-obj basin으로 보냄(portfolio도 증폭). 지금은 끝에서 2개(탐색 전/후 best)만 진짜채점. 이걸 **탐색 도중 여러 체크포인트의 best를 진짜채점**으로 늘려 best 제출 → "더 search ≥ 덜 search" **단조 보장** → prob_20/prob_7(+48%)/prob_6 drift 회귀를 구조적으로 차단. 이게 되면 (a) reserve 인하로 Gurobi headroom 환전, (b) adaptive-reserve(−9.1%) 안전 활성, (c) portfolio 워커도 단조 → 전부 잠금해제. **주의**: proxy 순위 top-K만 모으면 true-good이 누락되니 **시점 체크포인트** 기반. 설계 doc 권장.
0★b (MIP-repair 회귀 제거 — 입증 대기). **입력 기반 MIP-repair 게이트** — front-load MIP은 손실 인스턴스(prob_37 +10.9% 등)에서 탐색 8s를 헛되이 강탈. **싼 신호로 분기해 안 도울 인스턴스는 skip → pure-upside 회복.** 신호 2종: ① **GO=Z2/Z3 여유**(선호 vs 부하균형 긴장; 프록시 = bay수 m, a_pref의 Z2 불균형), ② **SKIP=Z1 난이도**(면적 utilization 높으면 MIP의 Z1-무시 배정을 repair가 못 잡음; [[train-tardiness-is-geometric]]). **가장 신뢰도 높은 방식 = 값싼 MIP probe**: MIP solve 자체는 싸다(~1-2s, 배정문제) — 짧은 TL로 풀어 surrogate(Z2+Z3) 격차 `(S_pref−S_mip)/S_pref`를 보고 임계 미만이면 repair(비싼 ~4s) 자체를 skip → 손실 인스턴스 비용 8s→~2s. **선결: cheap-falsification** — 40개에 신호값 vs 실제 best-of 승패를 찍어 분리되는지 확인(probe ~1분, 정적feature 공짜) 후 임계 추출·게이트화. 분리 안 되면 포기.
0★c (single-use 라이선스 후폭풍 — 검증 필요). **portfolio recombine 손상 재검증** — [[gurobi-single-use-license]] 때문에 T≥180 portfolio의 워커 4개 중 **먼저 라이선스 잡은 1개만 recombine 성공**, 나머지 3개는 조용히 no-op(크래시 X, 탐색 다양성은 유지). −28/−35% T=300 검증은 **Gurobi 전환 전(ortools, 동시 CP-SAT 무제한)** 측정이라 현재 실제 효과 불명. 옵션: (a) 현 상태 재측정(워커 3개 recombine 없이도 net 이득인가), (b) Gurobi 접근 직렬화(락/순번), (c) 워커는 recombine 없이 탐색만 + master가 1회 recombine. **eval timelimit 미공개**라 T≥180이 실제 경로면 영향 큼.
0. **full-20 재확인 + 기본값 플립** — 스택에 numba 기본 on(=−28.5% 추가). 전 20개로 새 합산·회귀 재확정 후 mask-search/자동조절/numba를 default on으로 코드 플립 + submission 폴더 갱신.
0c. **guided swap move** — 현재 이웃은 relocation(a→b)뿐, swap 없음. exit-time 신호(extract_tardiness의 `exited`, 현재 버려짐)로 tardiness↔slack / 무거움↔가벼움 / 상호선호불일치 3계열 top-K 쌍만 평가(랜덤 X). relocation이 못 넘는 Z2 미세조정·포화 bay 교환·더 깊은 local optimum 도달. **behavior-changing → eval-count 고정 obj-delta로 판정**(swap도 1 eval로 카운트, 공정). **품질 검증은 incremental-eval 없이 지금 가능**(full-eval+eval-count 결정론), incremental-eval은 배포 속도 곱셈기. 가장 싼 첫 probe = guided-swap Destroy 프로파일(포트폴리오 멤버). 설계 `docs/swap_move_experiment_design.md`.
1. **adaptive R (입력 기반 R4/8/16 자동선택)** — instance마다 최적 R 다름(prob_3은 R16까지 개선, prob_20은 R4로 충분). 입력에서 싼 신호로 판단: over-rejection 수렴(knee) / footprint·AABB 면적비 / 크기·예산. 먼저 측정: 전 20개 oracle-best R + 예측신호 상관 → 규칙화. (eval-제한적이라 거친 instance에 낮은 R = 더 빠름 = 더 좋음.)
2. **남은 속도 (eval-제한적이라 큰 objective 전환)** — (a) **mask precompute ~39% 가속**(shapely 래스터화 → numba point-in-poly/matplotlib path, supercover 안전성 재검증 필수), (b) **find_slot_mask 스캔 coarse numba화**(현재 호출당 → 통째 jit로 경계비용 제거).
2b. **C++ packing kernel 실험** — 전체 C++ 포팅이 아니라 `solve_bay` 하위 hot kernel(`masks_overlap`, `find_slot`, `find_slot_mask`, 이후 `_local_mask`)만 native로 옮겨 Numba cold-start를 제거할 수 있는지 검증. **pure speed → fixed-eval bit-identical 필수**, native packaging 허용 여부 먼저 확인. 설계 `docs/cpp_packing_kernel_experiment_design.md`.
3. **prob_3 회귀 원인 규명** — eval-count 무관(numba로도 불변). R/basin 문제로 추정.
3. **재실험 #3: H2 search→recombine→search 루프 under mask-search** (얇은 AABB pool이 폐기 사유였음). **이번 진단(아래 완료)으로 약화**: 실제 사이클 수가 어려운 instance(prob_13/14/20)에선 60초 1회·300초 1~2회뿐(climb가 루프 예산을 통째로 먹어 비수렴). 작은 instance만 12~83회. 즉 "정작 중요한 instance엔 루프 안에 recombine 끼울 자리가 거의 없음" → 하려면 사이클 기아부터 풀어야.
4. **탐색 중 incumbent 스냅샷 진짜채점** (어긋남 보정 — **자동조절 채택의 관문, 최우선**) — 지금은 끝에서 2개(탐색 전/후 최선)만 진짜 점수로 비교. 이걸 **탐색 도중 여러 시점의 최선 답 스냅샷**으로 늘려서 전부 진짜채점하고 best 제출. 그러면 "더 탐색"이 "덜 탐색(=이른 스냅샷)"보다 절대 나빠질 수 없음 → **단조 보장 → prob_7(+48%)·prob_6 회귀를 잡음.** (주의: proxy 순위 top-K만 모으면 true-good이 proxy 순위 낮아 누락될 수 있으니, **시점 체크포인트** 기반이 안전.) 이게 되면 자동조절(−9.1%)을 안전하게 켤 수 있음.
2. **`find_slot` 추가 가속** (numba 기본형 ~2× 확보 후) — 호출당 오버헤드 제거: `ov_boxes`를 numpy로 사전계산/버퍼 재사용, 또는 `solve_bay` 단위로 더 굵게 jitting해 경계 비용 분산. (behavior-invariant, 평가횟수 고정으로 검증.)
3. **polygon "상금" 측정** — `SOLVER_NOPOLY=1` vs 기본, full-20 @ T=180. 차이 = polygon이 지금 회복하는 양 = "더 나은 기하 테스트"가 노릴 상금의 하한. 싸다. 4번 진행 여부를 가름.
4. **거의 정확하면서 빠른 기하 테스트** (멀티박스/래스터) — 탐색과 최종을 한 기준으로 **통일** → 어긋남 제거 + AABB가 못 보던 맞물림 배치까지 탐색. 큰 보상·큰 위험. **보수적 유지 필수**(겹치는데 안 겹친다고 하면 실격 = feasibility). 2번 상금이 크면 진행.
5. **병렬 재시작 모음** (multiprocessing, ≤4코어) — 독립 ILS 여러 개 동시에 → 진짜 점수로 best-of. 탐색 중 노는 3코어 활용. 4로 캡(`os.cpu_count()`는 cpulimit 환경에서 못 믿음). 1번과 곱셈 효과.

---

## 🟢 완료 (최근 — 결론 + 링크)

- **순수 속도: mask-marshal 재구조 — 무거운 인스턴스 −3% (사용자 가설 확인), but rank-marginal → 미채택** (06-29) — 사용자 "순수 속도 개선?" → 현 빌드 cProfile(T20 E2500, post-numba/prepare/bbcache). **이미 적용 확인**: numba find_slot/scan/masks_overlap·shapely.prepare·bay 단위 캐시(2500eval→solve_bay 3033회=고도 증분)·per-instance bbox 캐시·`_LOCAL_MASK`/`_local_box` 캐시·bbcache → 통상 쉬운 레버 소진. cProfile self-time 1위=`_find_slot_mask_jit` 마샬링(13.4s/27%)으로 보여 **증분 마샬링 리팩터**(`_MaskBuf`로 placement당 1회 append + numba in-kernel 시간필터, O(n²)→O(n)) 구현. **전부 bit-identical**(T1/T13/T20/T37/T38/T39 obj 정확 일치). **1차 wall(E5000 작은 인스턴스): T20 +1%·T13 +3.7% 느림 → 일단 revert.** ▶**사용자 지적 "≥180s엔 효과 있지 않나?" → 대형 인스턴스 재측정(E1000 warm)**: **T38(530s, 초heavy) −3.3%·T37(141s) −3.2%** ✅, T39(215s) +0.9%·T19(5.8s) +4.7%·light +1~3.7%. **가설 확정**: 이득은 블록수가 아니라 **mask-escalation 빈도**(빽빽→AABB 자주 실패→마샬링 O(n²))에 비례 — T37/T38처럼 eval-starved(300s에 ~600eval)인 가장 무거운 인스턴스(=LAHC 약점 P4-P6류)에서 −3%, mask-light는 미세 손해(절대값 작고 수렴하므로 obj-harmless). **교훈 2건**: ① cProfile self-time은 Python 오버헤드 과대계상(마샬링 실 wall ~3%이지 27% 아님) → 순수속도는 고정-eval wall A/B로 판정; ② **속도 레버는 작은 인스턴스만 보면 안 됨 — 무거운(eval-starved) 인스턴스서 뒤집힐 수 있음.** **결정: 미채택**(−3%는 [[cpp-port-rejected-coldstart-small]] "1-3% wall=rank-marginal" 기준 + light 회귀 + `_MaskBuf` 복잡도; bit-identical 무위험이라 재고 여지는 있으나 marginal). 남은 실표적은 **shapely crane-check**(`check_entry`+`check_exit` cum ~13s, 실 C work)뿐인데 feasibility-critical("Target D")이라 고위험. 도구 `tools/_profile_speed.py`·`_time_eval.py`·`_time_marshal*.txt`. [[eval-count-ab-protocol]]
- **[ALNS fork] best-of(config A ∥ lever) 포트폴리오 + margin 튜닝 — 제출 대기** (06-25) — gate-튜닝 2종 falsify(est/best_tot·a_pref Z1 둘 다 help/harm 분리 실패, 제출 0회 로컬) → trajectory best-of. `ALNS/alns_portfolio.py`: worker A=config A floor(Gurobi無)·worker B=lever, **worker가 자기 해를 직접 polygon-pack+스코어해 (score,ops) 반환, master는 pick-min만(즉시)**. `myalgorithm.py`는 timelimit≥180만 포트폴리오·미만 단일 config A. **margin 튜닝**(사용자 요구 ≤5s): serial re-pack(final_guard) 제거 + worker build_margin 낭비 해소 + worker safety 예산무관 ~1.5s 캡(`ALNS_SAFETY_FRAC=0.005`) + 포트폴리오 safety ~2s → **margin 23~34s→~3s**(T20@180/300·T11@180 전부 ~3.1s, config A@120 ~2s, overrun 0, feasible). 안전: best-of=config A floor로 회귀 0, 느린 worker는 gather_dl 종료+a_pref floor가 feasible 보장(overrun 없음). 산출물 `myalgorithm0625-bestof.zip`(6파일; BRIDGE portfolio.py 제거). → `memory/alns-seed-recombine-instance-split.md`
- **[ALNS fork] threshold-180 probe — 채점기 timelimit 지도 완성** (06-24) — `tl<180→infeasible` probe(`myalgorithm0623-probe180.zip`). 결과: P1·P2 infeasible, P3·P4·P5·P6 feasible → **P4/P6도 ≥180 확정**. 교차검증(P1/P2 -1, P3/P5 scored) 전부 일치=probe 신뢰. **timelimit 지도: P1≤60 / P2∈(60,180) / P3~P6≥180** (≥180 내부 세분 미측정). 부수 발견: ≥180 lever 반응 분류 — P3(−16.7%)·P5(−4.1%)=MIP-favorable 재현, P4=불안정(제출3서 +0.77% 악화, T20형), P6=3회 bit-identical(lever 무력). P4는 현 lever-gate 빌드 소액 회귀 리스크. → `memory/grader-p1-p6-distinct.md`
- **[ALNS fork] timelimit-gated lever 빌드 (gate T≥180) — 제출 결과: 회귀 0 순수 상위호환 ✅** (06-23) — **결과: P1=81,306(회복)·P2=128,068(±0)·P3=799,883(−16.7%✅)·P4=11.65M(±0)·P5=26,591,869(−4.1%✅)·P6=53.6M(±0).** gate가 설계대로 작동 — P3/P5 lever 발동·이득(≥180, MIP-favorable), P2 config A(짧은 예산), **P4/P6(최대 목적값) 변화 0=lever 무력**(거대 인스턴스 MIP가 gate 예산 ~4s 내 미해결 추정 → 남은 상금 후보). 채택 권장. probe로 P2~P6이 >60s임을 확인한 뒤, lever(MIP-seed+recombine)를 **긴 예산에서만** 켜는 게이트를 `ALNS/myalgorithm.py`에 적용: `timelimit≥180 → ALNS_MIP_SEED=1+ALNS_RECOMB=final`, 미만 → config A(baseline). **명시적 set(양 분기)** 으로 프로세스 재사용 시 lever 누수 차단(검증: 한 프로세스서 tl=180 on→tl=60 off 확인). 검증: T1@60 config A(mip_seed=0, obj=595,876=baseline 일치), T13@180 lever on(mip_seed=1, recomb=1, obj=580,446), 추출 zip 양분기 feasible. 목적: 리더보드 목적값을 config-A baseline(P2~P6) 대비 비교해 **실제 채점 인스턴스에서** lever 효과 측정. 위험: 긴-예산 인스턴스 일부 회귀 가능(T20형)이나 −1 없음·graceful fallback·baseline 저장돼 1제출로 복구 가능. 산출물 `myalgorithm0623-lever-gate180.zip`. → `memory/alns-seed-recombine-instance-split.md`
- **[ALNS fork] timelimit-probe 제출본 제작 + grader timelimit 미공개 대응** (06-21) — grader가 `algorithm(prob, timelimit)`에 넘기는 timelimit이 미공개라, `ALNS/myalgorithm.py`에 분기 추가: `timelimit ≤ 60 → {"operations":{}}`(빈 해 → 전 블록 미배정 → Stage1 infeasible → 리더보드 −1), `> 60 → 평소 config A 실행`. **all −1이면 grader ≤60s, 정상 점수면 >60s**를 역추론. run-branch는 config A 고정(timelimit 탐지와 lever A/B 분리). 검증: tl=30/60 즉시 infeasible, tl=61/62/90 feasible(stage5), 추출 zip에서 양분기 OK. 산출물 `myalgorithm0621-probe.zip`. 게이트는 주석 표시(제거 시 plain config-A 복귀). **결과(06-23): P1만 infeasible, P2~P6 feasible → 채점기는 인스턴스별 timelimit 부여(P1≤60s, P2~P6>60s, 균일 아님). config-A baseline 확보: P2=128,068 / P3=960,756 / P4=11.65M / P5=27.74M / P6=53.6M. ⚠️ 채점 P1~P6은 로컬 benchmark 스위트·train(T*)과 별개 문제 → `memory/grader-p1-p6-distinct.md`, 네이밍 `memory/naming-train-T-grader-P.md`(train=T1~T40, grader=P1~P6).**
- **[ALNS fork] seed(MIP_SEED)+recombine lever A/B — 인스턴스마다 갈림, blanket default 기각** (06-21) — config A(off) vs B(`ALNS_MIP_SEED=1`+`ALNS_RECOMB=final`), 단발 SEED=0. **T180**: T11 **−52%**·T13 **−41%**(둘 다 MIP-favorable, recomb_win 포함) vs T20 **+8%**·T1 **+53%**. **T=60은 1승3패**(lever는 긴 예산에서 더 삼 — T13 +1.8%→−41% 뒤집힘). **T1 = 확정적 trap**: B가 T=60·T=180 정확히 684,219 동일(MIP-seed가 저-Z1 쉬운 인스턴스 탐색을 basin에 가둠, A는 예산 늘수록 개선) → **MIP-seed gate가 너무 느슨**. (train T* 측정 — 채점 P1~P6엔 직접 전이 안 됨) 결론: [[guided-destroy-portfolio]]와 동일 패턴 — default 말고 **best-of(A,B) 포트폴리오 멤버**로. 도구 `tools/_seed_recomb_ab.py`, 출력 `tools/_ab*.txt`. → `memory/alns-seed-recombine-instance-split.md`, `memory/gurobi-grader-license-portability.md`(grader 자체 Gurobi 라이선스 보유 확인)
- **recombine 풀 prune를 Z2/Z3-가중 cost로 — 채택 (기본 ON)** (06-19) — 발단: portfolio recombine 저하 수정 시도 중 union prune 진단. **핵심 발견**: 풀 prune가 **tardiness만**으로 잘랐는데(`(T,-coverage)`), 목적함수는 **Z3 지배**라 T 낮고 Z3 나쁜 조각을 keep하고 **타일링에 필요한 중간-T/저-Z3 조각을 버림** → prune이 빡빡하면 **tardiness-prune은 recombine이 아예 발동 못 함**(격리 테스트 `_prune_iso.py`: prob_6 per_bay=150/250서 개선 0). **해결**: prune key를 recombine **컬럼 cost(`w1·T + w3·pref_loss`)** 로(`_pool_prune_key`, `SOLVER_POOL_PRUNE_COST` 기본 1; =0이 legacy). **검증**: 격리(prune 강제발동) prob_6 **−47%**·prob_13 **−25%**, **cost ≤ tardiness 전 케이스**(절대 회귀 없음). production 단일프로세스: **T=60 bit-identical**(풀<per_bay·m → prune 미발동), **T=180 회귀 0** + **prob_20(m=5, 큰 풀) −6.1%**(670,426→629,591 — proxy-drift 희생자 개선!). 풀이 클수록(큰/많은-bay·긴 budget·portfolio 워커) 효과↑. 작은 풀 구간은 무변. → `memory/recombine-cost-prune.md`
- **portfolio recombine 저하 수정 — 전부 기각 (현 degraded가 최선); 부산물 cost-prune·NoRel 지식** (06-19) — single-use Gurobi([[gurobi-single-use-license]])로 T≥180 portfolio 워커 4개 중 recombine 1개만 성공. 수정 4종 전부 현 상태보다 못하거나 비효율: ① **solo**(워커0만 recombine, 1–3 검색환원): prob_20 **+18%**(환원 검색이 proxy-drift, 0★ 벽 재확인). ② **master union recombine**(워커 풀 반환→master 합집합 1회): regress. **정밀진단(`tools/_union_diag.py`)**: union 실패는 **prune도 incoherence도 아님** — 최선 per-worker cover가 union에 **representable=True**(조각 다 있음, union 4.5k·13k 둘 다), 진단 검색강도에선 union=per-worker 동일. T=180 회귀는 강-검색에서만 나오는 **incumbent/휴리스틱 민감성**(큰 union을 강한 best-search incumbent로 seed시 NoRel이 근처 해에 안주). ③ **NoRel**(`NoRelHeurTime`, 작년1등 >10k var 레버): 큰 union MIP를 살림 — union 8s=참사·20s=동률·**20s+NoRel prob_20 −15.6%(좋은 basin 고정)**. 큰 set-partitioning에 유효, 기억해둘 것. ④ **additive**(워커 recombine 유지 + master union을 *추가* best-of 후보): never-worse(prob_6 +28%→104k 복구)·prob_20/19 승, **그러나 master recombine ~15-20s 직렬 추가비용** → **효율로 기각**(워커 4 recombine은 이미 병렬·1스레드핀=~1개 wall이라 비용 아님; 추가 master가 비용). **→ master recombine 전부 제거, 현 degraded portfolio + cost-prune만.** `tools/_port_recomb_ab.py`·`_union_diag.py`. → `memory/portfolio-recombine-degraded.md`
- **MIP-propose + ILS-repair 통합 — front-load 직렬 채택 (sidecar·tail 기각)** (06-19) — 개념: 목적함수가 Z3-지배([[objective-z3-dominant]])라 레버는 배정 자체. 기존 ILS는 `a_pref`(Z1≈0, Z3 greedy최소)에서 얕게 Z2/Z3 개선만 → **방향을 뒤집어** Gurobi로 `min(w2·Z2+w3·Z3)` 배정(fits 제약, Z2 min-max 선형화)을 풀고 local_search로 Z1을 repair → best-of 가드 후보(`_mip_repair`). 작년 1등 MIP중심 모티프. **배치(placement) 3종 실험**: ① **parallel sidecar** 기각 — **결정적 발견: Gurobi WLS가 single-use**(`tools/_grb_concurrent_probe.py`로 직접 확인, 10009 'Another Gurobi process running'). sidecar가 라이선스 쥔 동안 부모 recombine가 조용히 no-op → recombine-의존 인스턴스 폭망(prob_40 +44%, MIP이 아니라 recombine 파괴가 원인). ② **tail(recombine 뒤 직렬)** 기각 — 최대 수혜 prob_20(300×5)은 idle tail이 ~0이라 8s MIP 미발동 + prob_40 **overrun(−1)**: 직렬에선 큰 수혜를 이기려면 탐색을 뺏어야 하므로 **pure-upside 원리적 불가**. ③ **front-load 채택** — 탐색 deadline이 t0기준이라 ~8s만 소모, **모든 수혜 발동(prob_20 포함)·overrun 없음**. **검증 T=60 clean seq**(control wall-noisy ±30%, mip출력은 결정론): prob_11 **−58%**·prob_13 **−27%**·prob_20 **−9%**(8s론 1500-var MIP 시간부족, 병렬이면 −76%였으나 라이선스로 불가), prob_37 **+10.9%**·prob_26 **+5.7%**·prob_40 **+0.7%**(탐색 8s 강탈), 전부 feasible·overrun無(≤58.2s). rank-based([[leaderboard-rank-based]])라 큰 high-Z3 승리가 소폭 손실 압도 기대. 코드: `_mip_repair` + `framework_solve` front-load 블록, `myalgorithm`에 `SOLVER_MIP_REPAIR=1` 기본 ON(단일프로세스 경로; 워커 제외). 도구 `tools/_mip_ab.py`·`_mip_tail_ab.py`. → `memory/gurobi-single-use-license.md`, `memory/mip-repair-serial-front-load.md`
- **find_slot_mask 스캔 numba 포팅 — 채택, −1 overrun 해결 + 큰 인스턴스 −3~40%** (06-18) — prob_40 cProfile이 가정을 뒤집음: 병목은 마스크 빌드(shapely)가 아니라 **`find_slot_mask`가 `masks_overlap`을 ~10억 회 호출(Python↔numba 경계비용)** = numba0≈numba1의 진짜 이유 + prob_40 overrun(−1)의 원인. **해결**: 한 orientation의 위치 스캔 전체(+overlap)를 단일 jit `_scan_mask_orient`로 이동(present 마스크를 padded uint64 배열로 1회 marshaling; check_entry는 Python에서 resume; AABB 프리필터·first-fit 순서 동일). **bit-identical 게이트 통과**: numba1=numba0 obj 정확 일치(prob_13/20/6/40, E=2500). **payoff(T=60)**: **prob_40 66.6s OVER → 57.8s (−1 해결) + obj −40%**(6.19M→3.74M), prob_38 −28%·prob_37 −22%·prob_23/26 −10%·다수 −3~5%, 전 인스턴스 feasible·overrun 없음. 원리: 빠른 패킹 = 같은 wall에 eval 폭증 = Z3 급감 → **eval셋급 큰 인스턴스(P4~P6류) 직격**. 순수 numba(OS 리스크 0). zip 갱신(solver.py 73KB). C++ 불필요 재확인(병목이 numba 커널이지 shapely 아니었음). → `memory/find-slot-mask-numba-scan.md`
- **C++ 커널 포팅 — cold-start 측정 후 기각** (06-18) — 설계문서 Section 9(가치 게이트)를 Windows에서 측정(native 빌드 전 선결). Numba cold-start: warm cache(정상 per-instance) prob_1/13/20 = **0.16/0.31/~0s**, cold cache(첫 인스턴스 풀 JIT 컴파일) prob_13 **0.63s** = wall(12~23s)의 **~1~3%**뿐. C++가 제거하는 건 이 cold-start 하나라 Linux x86_64 빌드+OS/packaging 리스크 대비 수지 안 맞음. **게다가 numba0 ≈ numba1**(prob_13 14.5 vs 14.43) → 현 mask-only 경로선 **numba 커널(Target A/B/C)이 병목이 아님**(진짜 병목은 shapely 마스크 래스터화 ~39%, numba 아님; 유일한 가치 타깃은 float-기하 Target D인데 bit-identical 어렵고 feasibility-critical). **→ C++ 포팅 보류**(rank-based 채점서 1~3% wall은 등수 영향 미미; effort는 등수 climbing/보고서 novelty에). 부수발견: 현 경로 numba 가치 재점검 필요. 권한은 organizer가 "컴파일 제출 허용"으로 확인됨. → `docs/cpp_packing_kernel_experiment_design.md` Gate0
- **idle-ILS (idle tail 회수) — monotonic, 기본 ON 채택** (06-18) — 발단: T=60서 각 인스턴스가 ~42s만 쓰고 ~30% idle(Gurobi/numba 가속 후 reserve가 과대). 발견(전 20개 분해 `.claude/scratch/obj_breakdown.log`): **목적함수의 ~80-99%가 Z3(선호 매칭)**, Z1(tardiness)는 17/20서 0, prob_1/9/14만 ~17%/4%, Z2 작음 → 비용은 거의 다 assignment 수준. 해법: recombine 후 남은 wall에 **현재 best에서 guarded ILS append**(`SOLVER_IDLE_ILS`). **핵심: init/ILS 데드라인 불변 → 같은 궤적 연장 → best 단조 → 회귀 구조적 불가**(reserve 인하가 init 재편으로 drift한 것과 정반대). **검증 T=60**: 6/6 페어 idle≤control(monotonic; drift났던 prob_20도 **−0.4%**), prob_11 **−14.8%**·prob_12/6 **−6%**·prob_14 −1.8%, 전 20개 feasible·overrun 없음(52~55.5s). `myalgorithm` 기본 ON(단일프로세스 경로; portfolio 워커는 코드 게이트 제외 → T<180 보완, T≥180은 portfolio). **idle 창 조임(06-18 추가)**: 남는 idle을 ~17초→**~3초**로. `idle_dl = poly_deadline - _build*2.5`(`_build`=직전 1회 실측 빌드비용; a_pref 과대추정 reserve 대신). 마진이 가드의 빌드 2개(best→base)를 안 자르게 해야 함(자르면 AABB fallback→Z1↑→monotonic 깨짐, margin 1.3서 prob_11 +9.9% 관측). self-adjusting(느린빌드=자동 더 예약). 재검증 T=60 margin 2.5: prob_20 monotonic·prob_11 −19%·prob_14 −10%, leftover ~2.5초, 전부 feasible·overrun無. 게이트 버그(`"0"` truthy) 수정. zip 4파일 갱신. → `memory/idle-ils-adopted.md`, `memory/objective-z3-dominant.md`
- **최종빌드·recombine 가드 mask-only 통일 — 채택(bit-identical), 단 drift 원인 아님** (06-18) — `_score_and_pack`·`_bestof_obj`를 SOLVER_MASK 시 best-of(AABB,mask)→**mask 단독**으로(탐색 proxy와 완전 일치, submission+baseline). **진단**(`.claude/scratch/aabb_vs_mask_*.log`): 전 20개 E=2500서 shipped assignment의 **어느 bay도 AABB가 mask를 못 이김(aabb_win_bays=0)** → best-of는 이미 死코드 → mask-only는 **전 20개 bit-identical**(회귀 0, feasible). 이득: 최종/가드 패킹 **2회→1회(~2배 쌈)** = wall서 truncation↓·탐색시간↑. **정정**: "탐색=mask vs 빌드=best-of seam이 drift 원인"이라던 가설 **반증** — seam이 휴면(AABB 死)이라 mask-only로 바꿔도 prob_20 reserve-인하 drift **+3.5% bit-identical 재현**. drift 진짜 원인은 metric 불일치 아님(탐색·빌드·가드 이미 mask로 일치) — **예산 재분배가 탐색 궤적을 바꾸고 가드가 시점 2개(best·base_incumbent)만 봐 둘 다 같이 drift**. 해법은 monotonic 다중체크포인트 가드(계획 0★, 재확인). zip 4파일 갱신. → `memory/proxy-drift-not-from-build-metric.md`
- **recombine reserve 인하 — REJECT (proxy-drift, headroom 환전 불가)** (06-18) — headroom sweep 후속 wall A/B(`.claude/scratch/reserve_ab_*.log`). 후보: reserve를 줄여(`SOLVER_RECOMB_CAP` 10→4/6, `FLOOR` 5→3/4, `SOLVE_S` 8→4/8) reclaim한 search로 환전. **T=60 단일**(reserve 비중 최대): prob_11 **−18%**·prob_6 **−15%**·prob_12 0%지만 **prob_20 +3.5%(3회 bit-identical = 결정적 회귀)**, candidate가 wall +6s(=reclaim 작동 확인). **T=300 portfolio(배포 구간)**: prob_20 control 543,440 → candidate(CAP4) **+42.7%**(775,440), 완만 candidate2(CAP6/SOLVE_S8) **+10.7%**(601,531). 완만판이 starvation은 제거하나 **+10.7% 잔여 = 순수 drift**. **결론: reserve 값 튜닝으로 못 고침** — 환전한 search가 binding 인스턴스 prob_20을 더 나쁜 true-obj basin으로 drift시키고, **portfolio는 흡수 아니라 증폭**(워커 전원 동시 drift → best-of 무력). **Gurobi headroom은 진짜지만 환전 불가** — adaptive-reserve를 막는 동일 proxy-drift 벽. → **이 실험이 monotonic incumbent-snapshot 가드(계획 0★)가 모든 '더 많은 search' 레버의 선결 전제임을 직접 입증.** 현 default(FLOOR5/CAP10/SOLVE_S8) 유지, 제출 변경 없음. → `memory/gurobi-migration.md`
- **Gurobi headroom sweep — H1(풀) 반증·H2(캡) 확정** (06-18) — recombine이 이제 Gurobi라, CP-SAT 시절 두 캡을 frozen-pool 재실행(`.claude/scratch/gurobi_headroom.py`)으로 재검토. prob_20/6/12 E=10000(큰 풀 11k+컬럼). **16셀(POOL_PER_BAY 1000~무제한 × RECOMB_SOLVE_S 1~8)이 인스턴스마다 전부 동일 obj·≤2.2s wall**; obj는 보드 기존값과 정확 일치(prob_20 618,260·920,860, prob_6 −47%, prob_12 무개선) → 검증됨. **H1(풀): 반증** — 무제한 풀도 1000캡과 같은 품질(recombine ~1000컬럼 plateau). 단 Gurobi가 무제한(11k)도 ≤2.2s에 풂 → `POOL_PER_BAY=1000`은 더이상 solvability 버팀목 아님(CP-SAT 목발 제거), 무해해 유지. **H2(캡): 확정** — Gurobi가 최난 prob_20도 ≤2s 최적증명(solve_s=1=8 동일), CP-SAT 8~12s 대비 ~10x → `SOLVER_RECOMB_SOLVE_S=8`은 완전 slack. **핵심 뉘앙스: 캡만 낮추면 효과 0**(Gurobi 이미 ~2s 조기종료) — 실이득은 `recombine_reserve`(FLOOR 5/CAP 10) 인하로 search-stop을 늦춰 reclaim. 출력 `.claude/scratch/gurobi_headroom_*.log`. → 다음 계획 0★로 이어짐. → `memory/gurobi-migration.md`
- **병렬 search portfolio (V1) 채택 — timelimit-gated 기본 ON (T≥180)** (06-18) — 노는 3코어 활용: 4개 다양화 프로파일(default / guided / mixed-guided / late-perturb, `SOLVER_SEED` 다양화)을 spawn 멀티프로세싱으로 독립 실행 → master가 `_score_and_pack` 한 기준으로 [seed + 각 워커 best] 진짜채점 best-of. 워커당 `SOLVER_CP_WORKERS=1`+`SOLVER_POOL_PER_BAY=1000`+threadpool 핀(4코어 예산 내). 구현: `submission/portfolio.py` + `framework_solve(_return_assignment=True)` 워커모드 포팅 + `SOLVER_SEED` 지원(기본 경로 byte-identical). **핵심: 긴 timelimit 전용 레버.** T=300 검증 prob_11 −35%·prob_20 −28%·prob_19 −15%·prob_12 −14%·prob_6 −8%; **T=180 prob_20 −5.8%(이번 측정)**; 그러나 **T=60은 회귀**(prob_20 +29%: 예산 4분할로 워커당 시간부족 → recombine 구제 실패, prob_11 무승부). → `myalgorithm`에서 **timelimit ≥ `SOLVER_PORTFOLIO_MIN_T`(기본 180)일 때만 활성**, 미만은 단일프로세스. mp 불가/전원 워커실패 시에도 단일프로세스 fallback(평가기가 subprocess 막아도 안전). 제출 zip = 4파일(+`portfolio.py`), 추출본에서 spawn+feasible+no-overrun 검증 완료. 설계 `docs/parallel_search_portfolio_experiment_design.md` → `memory/parallel-portfolio-adopted.md`
- **ortools CP-SAT → Gurobi 전환 (3개 MIP 전부) + recombine cap 판단 — 전환 완료, cap 8s 유지** (06-18) — `submission/solver.py:_recombine`·`baseline/solver.py:_recombine`·`baseline/covering_solver.py:_solve_covering`의 set-partitioning/covering MIP를 **Gurobi**(gurobipy 13.0.2, Academic WLS 라이선스, 크기 무제한)로 교체, ortools를 코드·`ogc2026_env.yml`에서 완전 제거. 패턴: 프로세스당 lazy 공유 env(`_grb_env()`, OutputFlag=0 → 배너·로그 억제, 라이선스 1회 체크아웃), covering 루프에서 `model.dispose()`, env-var 이름 유지(`SOLVER_RECOMB_SOLVE_S`→TimeLimit, `*_CP_WORKERS`→Threads, `random_seed`→Seed), AddHint→`var.Start`(웜스타트), status→`SolCount>0`, `_HAS_ORTOOLS`→`_HAS_GUROBI`. 스모크(prob_1): submission/baseline recombine feasible(obj 335,848 동일), covering 19회 생성+dispose feasible(255,694). **cap 판단(재실행 없이 기존 `.claude/scratch/recomb_cap_sweep*.log` 분석)**: 점수는 cap의 **계단함수**(개선분 전부 회수 or 0), 8s는 **binding 인스턴스 prob_20 한 개**가 정함(CP-SAT서 −20~34% 회수에 ~8~14.5s 소요), 쉬운 건 일찍 자동 종료(prob_12 5.5s에 최적증명). Gurobi 속도이득은 cap 유지해도 **조기종료로 자동 회수**되고, cap을 8s 미만으로 낮추면 prob_20 개선분만 잃을 위험(가드 덕에 회귀는 없음, 이득 포기만) → **8s 유지가 기존 데이터 기준 정답**. 낮추려면 새 실험 말고 `recomb_cap_sweep.py`를 Gurobi·prob_20만 돌려 새 문턱 측정(또는 `MIPGap`/무개선중단 레버, 코드변경 별건). → `memory/gurobi-migration.md`
- **incremental(delta) search 평가 — 고정 eval 안전, 폐기** — Step 0: 기본 스택(mask-search + mask_prepare + numba) 후보 240개(prob_1/13/20 각 80) 프로파일에서 제거 가능한 부기(`dict copy + eval_obj1 bookkeeping + obj23`)는 **전체 6.39%**뿐, 큰 hard `prob_20`은 **4.83%**. `solve_bay`가 **93.61%**로 지배적. 추가로 임시 prototype(`SOLVER_INCR_EVAL=1`)을 넣고 fixed-eval 검증: **E=500 full20 mismatch 0**(wall −0.16%, noise), **E=2500 hard subset mismatch 0**이나 wall **+2.76% 느림**. 결론: 고정 eval 품질 악화는 없지만 속도 레버로는 채택 안 함. prototype은 `submission/solver.py`에서 제거. 출력 `.claude/scratch/incr_eval/{profile_numba,full20_E500_*,hard_E2500_*}.jsonl`, 상세 `docs/incremental_eval_experiment_design.md` §15–16.
- **recombine 3종 개선 (가드 mask화 + solve 8s 캡 + 예약/풀 캡) — 채택** — 발단: "in-loop recombine?" 진단 → recombine가 무겁고(60초 5~11s, 300초 17~32s) 사이클이 적음(어려운 instance 1~2회) + idle tail 폭증(300초 ~50s) 발견. 분해 결과 **3가지**:
  - **① 가드 mask화** (`_bestof_obj`: polygon→best-of(AABB,mask), 빌드와 일치): 가드가 제출과 다른 점수(polygon)로 채택 판정하던 **stale 버그**. mask는 polygon의 ~1/15 → 가드 prob_20 **7.0s→0.3s**. **full-20 eval-count(E=2500) A/B: 회귀 0, prob_11 −7.03%·prob_14 −7.20% 개선, 나머지 18 동일, 전부 feasible → 합산 −1.39%.** "지금까지 개선이 polygon 효과 아니냐"는 가설은 반증(norecomb·이 A/B 모두 TRUE 점수 기반).
  - **② solve 8s 캡** (`SOLVER_RECOMB_SOLVE_S`, max_time=min(8,남은)): cap-sweep 결과 recombine 점수는 **계단함수, CP-SAT ~8s에 전부 회수**(이후 16/30s 추가이득 0; prob_20 한계 8s). 기존 max_time=예약×0.5는 300초서 27s까지 낭비.
  - **③ 풀/예약 캡** (`SOLVER_POOL_PER_BAY` 기본 1000, `SOLVER_RECOMB_CAP` 기본 10s): 예약을 timelimit×0.18(300초→54s)에서 **절대 10s**로. **핵심 발견**: 긴 timelimit에선 풀이 거대(prob_5 83사이클→2만+컬럼)해져 **CP-SAT 모델구축·solve가 8s/예약을 초과** → recombine가 best 그대로 반환, **나쁜 incumbent를 구제 못 함**(prob_20 E=10000: 캡 없으면 1.87M = norecomb와 동일, **bay당 top-1000 캡 → 618k**). 캡이 MIP를 8s로 풀리는 크기로 되돌려 고예산에서 recombine가 실제 작동. (시행착오: 예약만 10s로 줄였다가 300초서 빌드가 poly_deadline 넘겨 AABB로 잘려 prob_5 **817k·overrun** → 원인이 풀 크기임을 규명 후 풀 캡으로 해결. safety=2 시도는 overrun으로 기각.)
  - **검증**: 전 20개 E=2500 known-good과 **bit-identical**·전부 feasible(60초/저예산은 풀 작아 캡 미발동); prob_20 **E=6000 결정론 NEW=OLD=766,341(회귀 0)**; 300초 wall에서 817k/overrun 소멸(prob_5/13 OLD와 동일, prob_20은 wall 변동); 패키징 zip smoke OK. submission/zip 동기화. 출력 `.claude/scratch/{cycle_recomb*,recomb_cap_sweep*,recomb_profile*,guard_ab,poolcap_eval,p20_resolve,newcfg_300,finalgate}`.
- **recombine 제거 실험 (`SOLVER_NORECOMB`) — 제거 불가, 유지 확정** — eval-count 고정(E=2500) A/B: 제거 시 **prob_6 +92.5%·prob_20 +29%·prob_12 +21%** 치명적, prob_11/13/14/5는 무영향. wall은 ~10~14%(4~13s) 절감하나 대가가 너무 큼. recombine의 MIP 교차 bay-조각 재조합은 단일 탐색이 못 하는 일 → reserve 회수로 못 메움. **과거 "prob_20 recombine 0 개선" 노트 반증**(현 unified+mask 설정선 −29%). 새 eval-count 프로토콜 첫 적용. 출력 `.claude/scratch/norecomb/`.
- **mask precompute 가속 = `shapely.prepare` (`SOLVER_MASK_PREPARE`) — 제출 기본값 ON 채택** — 프로파일: 마스크 빌드의 **~92%가 셀별 점-포함 검사**. `shapely.prepare(buf)` 한 줄로 그 단계 **−78%(4.5x)**, 빌드 총량 prob_20 21.6s→6.1s(**−72%**), **mismatch=0 = bit-identical**(feasibility 보존). **eval-count 고정서 obj 완전 동일**(prob_13 584,454·prob_20 920,088 on/off), wall만 −5~11%(짧은 예산선 더 큼)→evals↑. **앞선 wall 게이트의 "회귀"(prob_12 +38.8% 등)는 proxy-drift가 아니라 wall run-to-run 변동(20~66%) 노이즈였음**이 eval-count 재측정으로 확정(같은 설정 3회 bit-identical). → 순수 속도 레버, 품질·feasibility 무위험. commit 81d620b. 출력 `.claude/scratch/{mask_profile,prepgate,var}/`.
- **guided destroy @ mask-search (`SOLVER_GUIDED=1`, unified 하) — 전역 기본값 반증, 단 강력한 portfolio 멤버** — full-20 paired(T=60): net **−1.4%**(9승8패3무)지만 양방향 극심한 변동(prob_4 **+48%**, prob_7/12 +37%; prob_12는 plain unified −24%승→guided +37%로 파괴). **가설 절반 적중**: 과거 AABB-guided 회귀(prob_17/18)는 mask-guided에서 중립/−0.8%로 flip — 하지만 새 회귀(prob_4/7/12)로 고통만 이동. **mix(50/50)는 net +1.5%로 더 나쁨(탈락).** **상보성 발견**: best-of(none, guided) = **−5.5% vs 현재 기본**(guided 승리 prob_9/3/15/1서 −20~36% 흡수). → guided는 **병렬 best-of portfolio 멤버**로 가치(전역 플립 ✗). R-다양성과 곱셈. **방법론 교훈**: 싼 7개 3-way A/B(−4.5%)가 경합 변동으로 오도(prob_6 −37%→실제 +21%) → full-20 paired만 신뢰. 출력 `.claude/scratch/gguide/`. → `memory/guided-destroy-portfolio.md`
- **통합 ILS 재구조화 (`SOLVER_UNIFIED_ILS`, INIT_FRAC=0.6) — 채택** — 기존 improved→local→ILS 파이프라인을 단일 루프(perturb→best-of(_climb, local_search)→accept)로 대체. legacy는 ils_dl=탐색끝이라 local이 윈도우를 다 쓰면 ILS가 ~0s 굶었음 → unified는 init 60%로 신뢰 incumbent(가드 바닥) 완전수렴 후 나머지 전부를 perturb 루프에 사용. **full-20 wall A/B(T=60): −9.05%, 8승4패8무, 전부 feasible·시간 내, 위험 회귀 없음**(최악 prob_5 +4.9%). 큰 이득 prob_12/6/11 −22~24%. **INIT_FRAC 0.4(−11.4%지만 prob_18 +37% 위험) vs 0.6(−9%, 안전) → 0.6 채택.** **step-count 종료(`SOLVER_UNIFIED_PATIENCE`) 반증**(큰 인스턴스서 완전수렴 우수; prob_5만 P100 도움 → knob만 유지, 미채택). 기본 경로 bit-identical 확인(prob_5/13/17). **→ 제출 기본값 on**(`submission/myalgorithm.py` setdefault). 출력: `.claude/scratch/uwgate/`.
- **병렬 portfolio R-sweep (8개, T=120)** — best-of(R4,R8,R16) = **−11.1% vs 단일 R8**, 전부 feasible. **최적 R이 instance별 전부 다름**(R4/R8/R16 각각 어디선가 이김, 양방향 큰 스프레드): R16은 fine-geometry(prob_3/6) 승·eval-starved(prob_11/20)서 악화, R4는 그 반대. **prob_3 회귀 R16이 거의 해결, prob_18 R4가 해결.** → R-다양성 portfolio 가치 확실 + adaptive-R 천장(=best-of-R, 예측 불필요·병렬로 직접 달성). → `memory/bitmask-collision-result.md`
- **numba payoff (wall A/B, T=120)** — 스택 numba off vs on: **합산 −28.5%**, 전부 feasible. **prob_18 회귀 해결+α**(+17.9%→기본보다 −50%, eval-count가 원인 확인), prob_13 −47%, prob_20 −16%. **prob_3만 불변(+0%)** = eval-count 무관 별도 원인. **→ numba 스택 기본 on.** 스택이 eval-제한적이라 속도→objective 전환이 큼(=남은 속도 표적 가치↑). → `memory/bitmask-collision-result.md`
- **numba `masks_overlap` (uint64 패킹) + find_slot AABB** — `_local_mask`에 uint64 rows 추가, numba `_masks_overlap_u64`, `masks_overlap`이 `SOLVER_NUMBA` 시 dispatch. **behavior-invariant 확정**(big-int vs numba 15000 케이스 R4/8/16 모두 mismatch=0; eval-count obj 완전 동일). 스택 end-to-end **~1.56× 속도**(prob_13 41→26s, prob_20 64→40s). masks_overlap precompute는 미가속(별개 표적). → `memory/bitmask-collision-result.md`
- **스택 full-20 확인 (채택 관문)** — 현재 기본 vs 스택(mask-search+자동조절 R8) @ T=120: **합산 −40.2%**(9.81M→5.87M), **18승 2패, 전부 feasible.** 회귀 prob_18 +17.9%·prob_3 +11.8%(둘 다 evals 1/4 손실). **→ 채택 확정**(기본값 전환). 역대 최고, default objective를 거의 절반으로. → `memory/bitmask-collision-result.md`
- **프로파일링 (prob_20 스택)** — 단계: seed 真값 5.14M → improved/local search(각 ~36s)가 1.61M(개선의 거의 전부), ILS 0s(예산 소진), recombine 11s 무익. 코드 점유: **mask precompute(shapely 래스터화) ~39% #1 비용**, masks_overlap ~12%, find_slot AABB ~9%, recombine 가드가 polygon 사용(~8s). → 최적화 표적: ①mask precompute(R↓/래스터화 가속) ②masks_overlap numba화 ③recombine 재검토. evals 회복이 prob_3/18 회귀 해소 경로.
- **재실험 #1: 자동조절 + mask-search** (A/B, T=120, 7개) — **drift 이론 확정.** AABB 때 회귀였던 prob_7(+48%→+0%)·prob_6(+18.5%→−68.8%)·prob_11(+23.7%→−15.4%)가 mask-search 하에선 회귀 소멸/개선. 자동조절이 mask-search 위에 **−12.5% 추가(회귀 0, 전부 feasible)**. → 과거 "X 회귀" 판정 재검증 프로그램 정당화. → `memory/bitmask-collision-result.md`
- **mask를 탐색 scoring에 적용 (A/B, T=120, 8개)** — `SOLVER_MASK_SEARCH`. 기본 vs mask-search 통합: **합산 −34.5%, 전부 feasible.** prob_20 −52.5%, prob_7(drift) −31.3%, prob_6 −31%, prob_11 −20%, prob_5 −18%; 회귀는 prob_3 +11.8%(evals 1/4 손실)뿐. **드리프트 해소 + 맞물림 탐색 = 변혁적 lever.** → `memory/bitmask-collision-result.md`
- **bitmask(supercover) 충돌 모델 구현·검증** (`SOLVER_MASK`/`SOLVER_MASK_R`, 기본 off) — 안전성 ✓(false-negative=0, R4/8/16), 최종빌드 A/B 전부 feasible + mask R8 ≈ polygon 품질. **속도(같은 배치 빌드, 정상상태): mask ~3–4× AABB vs polygon ~50–65× → mask가 polygon보다 ~15× 빠름.** precompute 일회성(R8 ~2–9s). → mask는 탐색에 쓸 만큼 쌈 ⇒ 계획 #0. → `memory/bitmask-collision-result.md`
- **`find_slot` numba 포팅** (`SOLVER_NUMBA`, env-gated, 기본 off) — 평가횟수 고정에서 **결과 bit-identical**(prob_5/13/17) = behavior-invariant 증명 ✓ (안전). 속도: search만 **~2.2×**(prob_13 NOPOLY 36→16s), polygon 포함 ~1.5×. 기대(10×)보다 작음 — **호출당 Python 전처리(ov_boxes bounding_rect + np.asarray)+numba 진입 오버헤드**가 천장. 안전한 ~2× 확보지만 transformative 아님. (로컬 .venv에 numba 0.65.1 설치; 평가 env는 0.61, 결과는 버전 무관.) 더 키우려면 → 계획 #1.
- **예비시간 자동조절 full-20 비교** (기본 vs poly만 자동조절, T=180) — 합산 **−9.1%** (8,594,557 → 7,814,220), 10승 4패 6무, 전부 feasible. 큰 이득: prob_19/11/20/13/14 (−13~17%). **그러나 어긋남 회귀 prob_7 +48%(!) · prob_6 +8%** → **단독 채택 보류.** 채택은 계획 #1(스냅샷 진짜채점)으로 회귀를 잡은 뒤. → `memory/bridge-third-long-budget.md`
- **prob_7 +48% 회귀 = 진짜 (variance 아님)** — 기본 106,613 ×3, 자동조절 157,916 ×3, **둘 다 0% 편차(완전 결정적)**. 더 많은 탐색이 재현 가능하게 48% 악화 = 심각한 어긋남. → 자동조절 채택은 계획 #1이 선결. → `memory/bridge-third-long-budget.md`
- **warm-start graft (BRIDGE → third 풀생성)** — **반증.** graft@180이 그냥 BRIDGE@180보다 4개 전부 나쁨(+10.2%). third의 승리는 자기 전체 탐색이 필요 → graft 아니라 portfolio가 맞음. → `memory/bridge-third-long-budget.md`
- **장기 budget 스케일링 + idle tail 원인** — BRIDGE는 T=300까지 미포화(prob_20 180→300 −32%). ~45% idle tail의 원인 = 가속 이전에 맞춰진 낡은 예비시간(poly 0.30 + recomb 0.18)이 탐색을 굶김. → 같은 메모리
- **예비시간 줄이기 (고정 저값)** — poly 0.10/recomb 0.07 @ T=300: 4개 합산 **−18.2%**, 전부 feasible. 레버 확인.
- **예비시간 자동조절 구현** (`SOLVER_ADAPTIVE_RESERVE`, env-gated, 기본 off) — poly 예비시간을 실측 build 비용으로 조절. recombine까지 조절하면 굶는 결함 발견·수정(poly만 조절; prob_11 +23.7% → −14%). **어긋남(proxy-drift) 발견**(prob_6 +8%, 재현됨): 탐색을 더 한다고 항상 좋아지는 게 아님. → 같은 메모리
- **standalone 최강 = BRIDGE** (vs third/covering). third는 상보적(T=180에서 prob_11/20 승). → 같은 메모리

---

*상세 과거 서술은 `docs/experiment_log.md` · `docs/third_algorithm_experiment_log.md` 참고. 이 보드는 라이브 인덱스.*
